import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prashikshit.manapranayam',
  appName: 'Manaprayanam',
  webDir: 'dist/manaprayanam',
  server: {
    androidScheme: 'https'
  }
};

export default config;
